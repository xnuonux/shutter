import http from 'node:http';
const key=process.env.FAL_API_KEY, token=process.env.SHUTTER_H3_LAB_TOKEN, port=Number(process.env.PORT||8080), budget=Number(process.env.SHUTTER_H3_LAB_BUDGET_USD||4);
if(!key||!token)throw Error('lab_missing_secret');
const queue='https://queue.fal.run/',pricing='https://api.fal.ai/v1/models/pricing';
const image='https://storage.googleapis.com/falserverless/example_inputs/hailuo23/pro_i2v_in.jpg';
const end='https://storage.googleapis.com/falserverless/example_inputs/interpolate-end-frame.png';
const priorVideo='https://v3b.fal.media/files/b/0aaa17fb/6WqqHmb8Kbj6dOo_AlIDB_minimax-h3.mp4';
const state={started:false,done:false,phase:'idle',error:null,startedAt:null,finishedAt:null,estimatedUsd:0,actualUsd:0,budget,prices:{},tests:[]};
const sleep=ms=>new Promise(r=>setTimeout(r,ms)),round=n=>Math.round(n*1e6)/1e6;
async function fal(url,opt={}){const r=await fetch(url,{...opt,headers:{Authorization:'Key '+key,'content-type':'application/json',...(opt.headers||{})},redirect:'error',signal:AbortSignal.timeout(90000)});if(!r.ok)throw Error(`fal_http_${r.status}:${(await r.text()).slice(0,500)}`);return r;}
async function price(ep){if(state.prices[ep])return state.prices[ep];const r=await fal(pricing+'?endpoint_id='+encodeURIComponent(ep)),j=await r.json(),p=(j.prices||[]).find(x=>x.endpoint_id===ep);if(!p||p.currency!=='USD'||p.unit!=='seconds')throw Error('price_missing:'+ep);state.prices[ep]=p;return p;}
function publicTest(t){return {name:t.name,endpoint:t.endpoint,state:t.state,estimatedUsd:t.estimatedUsd??null,actualUsd:t.actualUsd??null,requestId:t.requestId??null,video:t.video??null,expandedPrompt:t.expandedPrompt??null,seed:t.seed??null,timings:t.timings??null,error:t.error??null};}
const snapshot=()=>({...state,tests:state.tests.map(publicTest)});
async function one(spec){const t={name:spec.name,endpoint:spec.endpoint,state:'quoting'};state.tests.push(t);try{const p=await price(spec.endpoint),mult=spec.input.resolution==='1080P'?3.2:spec.input.resolution==='768P'?1.6:1,est=round(Number(p.unit_price)*spec.input.duration*mult);if(est>spec.ceiling||state.estimatedUsd+est>budget)throw Error('quote_ceiling');t.estimatedUsd=est;state.estimatedUsd=round(state.estimatedUsd+est);t.state='submitting';const sub=await fal(queue+spec.endpoint,{method:'POST',body:JSON.stringify(spec.input)}),rc=await sub.json();if(!rc.request_id||!rc.status_url||!rc.response_url)throw Error('receipt_invalid');t.requestId=rc.request_id;t.state='rendering';for(let i=0;i<180;i++){await sleep(4000);const sr=await fal(rc.status_url),sj=await sr.json();if(['FAILED','CANCELLED'].includes(sj.status))throw Error('provider_'+sj.status.toLowerCase());if(sj.status!=='COMPLETED')continue;t.state='fetching';const rr=await fal(rc.response_url),units=rr.headers.get('x-fal-billable-units'),out=await rr.json(),n=units===null?null:Number(units);const actual=Number.isFinite(n)?round(n*Number(p.unit_price)):est;t.actualUsd=actual;state.actualUsd=round(state.actualUsd+actual);if(state.actualUsd>budget)throw Error('budget_exceeded_actual');const v=out.video||out.data?.video||null;t.video=v&&typeof v==='object'?{url:v.url||null,file_size:v.file_size??null,content_type:v.content_type??null,file_name:v.file_name??null}:v;t.expandedPrompt=out.expanded_prompt??out.data?.expanded_prompt??null;t.seed=out.seed??out.data?.seed??null;t.timings=out.timings??out.data?.timings??null;t.state='ready';return;}throw Error('provider_timeout');}catch(e){t.state='failed';t.error=e.message;throw e;}}
async function run(){state.started=true;state.startedAt=new Date().toISOString();const base={prompt_expansion_mode:'quality',enable_safety_checker:true,sync_mode:false};const matrix=[
{name:'i2v-start-1080',endpoint:'minimax/h3-max/image-to-video',ceiling:.3,input:{...base,duration:5,resolution:'1080P',image_url:image,prompt:'Preserve the exact opening subject and composition. Slow cinematic push with natural physical motion. No cuts, no new subjects, no text.'}},
{name:'bridge-10s-480',endpoint:'minimax/h3-max/image-to-video',ceiling:.2,input:{...base,duration:10,resolution:'480P',image_url:image,end_image_url:end,prompt:'Create one continuous editorial bridge between these exact boundary frames. Use physically motivated occlusion, atmosphere or camera movement rather than a hard cut. Arrive naturally at the supplied ending frame. No text.'}},
{name:'camera-768',endpoint:'minimax/h3-max/camera-controls',ceiling:.2,input:{...base,duration:5,resolution:'768P',image_url:image,prompt:'Preserve subject and environment. Camera trajectory is the primary direction; no cuts or extra camera moves.',camera_trajectory:[{time:0,elevation:0,azimuth:0,distance:1},{time:.5,elevation:10,azimuth:35,distance:.82},{time:1,elevation:2,azimuth:70,distance:1}]}},
{name:'ref2v-two-images',endpoint:'minimax/h3-max/reference-to-video',ceiling:1,input:{...base,duration:5,resolution:'480P',aspect_ratio:'adaptive',reference_image_urls:[image,end],prompt:'Use Image 1 for action subject identity/materials and Image 2 for a secondary visual/world reference. Create a new coherent camera angle, preserving the reference traits without simply recreating either composition. No text.'}},
{name:'ref2v-prior-video',endpoint:'minimax/h3-max/reference-to-video',ceiling:1,input:{...base,duration:5,resolution:'480P',aspect_ratio:'16:9',reference_video_urls:[priorVideo],prompt:'Use Video 1 as continuity evidence for subject, wardrobe, materials, environment and motion language. Create a new adjacent shot from a distinct camera position while preserving those traits and plausible scene state. No text.'}}
];try{for(const spec of matrix){state.phase=spec.name;await one(spec);}state.phase='complete';}catch(e){state.phase='stopped-on-failure';state.error=e.message;}finally{state.done=true;state.finishedAt=new Date().toISOString();}}
function authorized(req){const u=new URL(req.url,'http://lab');return u.searchParams.get('token')===token?u:null;}
const server=http.createServer(async(req,res)=>{const u=authorized(req);if(!u){res.writeHead(403,{'content-type':'application/json'});return res.end('{"error":"forbidden"}');}res.setHeader('content-type','application/json');res.setHeader('cache-control','no-store');try{if(u.pathname==='/smoke'){for(const ep of ['minimax/h3-max/image-to-video','minimax/h3-max/camera-controls','minimax/h3-max/reference-to-video'])await price(ep);return res.end(JSON.stringify({ok:true,hasKey:true,budget,prices:state.prices}));}if(u.pathname==='/run'){if(!state.started)run();res.statusCode=202;return res.end(JSON.stringify(snapshot()));}if(u.pathname==='/status')return res.end(JSON.stringify(snapshot()));if(u.pathname==='/stop'){res.end(JSON.stringify({ok:true,state:snapshot()}));setTimeout(()=>server.close(()=>process.exit(0)),50);return;}res.statusCode=404;res.end('{"error":"not_found"}');}catch(e){res.statusCode=500;res.end(JSON.stringify({error:e.message}));}});
server.listen(port,'0.0.0.0',()=>console.log(JSON.stringify({type:'h3-lab-round2-ready',port,budget})));
